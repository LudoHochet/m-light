<div class="d-flex align-items-center">
<!-- btn new-project for user signed_in -->
  <% if user_signed_in? %>
    <%= link_to "new project", new_project_path, class:"new-project btn-white mr-3" %>
  <% end %>
  <!-- search zone -->
  <!-- <div class="d-flex align-items-center"> -->
    <%#= form_tag root_path(anchor: 'all-projects'), class:"form-inline d-flex justify-content-center md-form form-sm active-pink active-pink-2", method: :get do %>
      <!-- <div class="md-form my-0"> -->
        <%#= text_field_tag :query, params[:query], placeholder: "Find a project", id:"search-input", class:"form-control form-control-sm ml-2 w-77 d-none animated fadeInLeft"%>
      <!-- </div> -->
    <%# end %>
    <!-- <i class="fas fa-search icon-session search-icon"></i> -->
  <!-- </div> -->
  <!-- end search zone -->


  <!-- test nouveau search -->
    <div class="search">
      <%= form_tag root_path(anchor: 'all-projects'), class:"form-inline d-flex justify-content-center", method: :get do %>
          <div class="form-group d-flex justify-content-center mb-0">
            <%= text_field_tag :query, params[:query], id:"search-input", class: "d-none animated fadeInLeft", placeholder: "find a project" %>
            <i class="fas fa-search icon-session search-icon"></i>
          </div>
      <% end %>
    </div>
  <!-- fin du test -->

  <% if current_page?(about_path) %>
    <p><%= link_to "About Us", about_path, class:"links ml-3 active-filter" %></p>
  <% else %>
    <p><%= link_to "About Us", about_path, class:"links ml-3" %></p>
  <% end %>
  <% if current_page?(contact_path) %>
    <p><%= link_to "Contact", contact_path, class:"links ml-3 active-filter" %></p>
  <% else %>
    <p><%= link_to "Contact", contact_path, class:"links ml-3" %></p>
  <% end %>

  <div class="icon-session ml-3">
    <i class="fas fa-bars dropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"></i>
    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
      <% if user_signed_in? %>
        <% if user_signed_in? %>
          <div class="hidden-new-project">
            <%= link_to "new project", new_project_path, class:"nav-link" %>
            <div class="dropdown-divider"></div>
          </div>
        <% end %>
        <div class="hidden-links">
          <%= link_to "Contact", contact_path, class:"nav-link" %>
          <%= link_to "About Us", about_path, class:"nav-link" %>
          <div class="dropdown-divider"></div>
        </div>
        <%= link_to "Log out", destroy_user_session_path, method: :delete, class:"nav-link" %>
      <% else %>
        <div class="hidden-links">
          <%= link_to "Contact", contact_path, class:"nav-link" %>
          <%= link_to "About Us", about_path, class:"nav-link" %>
          <div class="dropdown-divider"></div>
        </div>
        <%= link_to "Login", new_user_session_path, class:"nav-link" %>
      <% end %>
    </div>
  </div>
</div>
