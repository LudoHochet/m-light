<%= render 'shared/banner' %>

<div class="container">
  <div class="filter-category d-flex justify-content-center">
    <h6 class="mx-3 category" id="tv">TV</h6>
    <h6 class="mx-3 category" id="live">LIVE</h6>
    <h6 class="mx-3 category" id="evenement">EVENT</h6>
    <h6 class="mx-3 category" id="fashion">FASHION</h6>
  </div>
  <% if @projects %>
    <div class="project-zone projects">
      <div class="cards">
        <% @projects.each do |project| %>
          <% if project.photos.attached? %>
            <div class="entire-card">
              <%= link_to project_path(project) do %>
                <div class="card-project draw" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url('<%= cl_image_path project.photos.first.key %>');">
                  <p class="project-name"><%= project.name %></p>
                </div>
              <% end %>
              <% if user_signed_in? %>
                <%= link_to project_path(project), method: :delete, data: {confirm: "certain de vouloir supprimer ce projet?"} do %>
                  <i class="far fa-times-circle delete-project"></i>
                <% end %>
              <% end %>
            </div>
          <% else %>
            <% if project.illustrations.any? %>
              <div class="entire-card">
                <%= link_to project_path(project) do %>
                  <div class="card-project draw" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url('<%= cl_image_path project.illustrations.first.photos.first.key %>');">
                    <p class="project-name"><%= project.name %></p>
                  </div>
                <% end %>
                <% if user_signed_in? %>
                  <%= link_to project_path(project), method: :delete, data: {confirm: "certain de vouloir supprimer ce projet?"} do %>
                    <i class="far fa-times-circle delete-project"></i>
                  <% end %>
                <% end %>
              </div>
            <% else %>
              <div class="entire-card">
                <%= link_to project_path(project) do %>
                  <div class="card-project draw" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url(https://images.unsplash.com/photo-1544981037-82284dcf0a0f?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=500&q=60);">
                    <p class="project-name"><%= project.name %></p>
                  </div>
                <% end %>
                <% if user_signed_in? %>
                  <%= link_to project_path(project), method: :delete, data: {confirm: "certain de vouloir supprimer ce projet?"} do %>
                    <i class="far fa-times-circle delete-project"></i>
                  <% end %>
                <% end %>
              </div>
            <% end %>
          <% end %>
        <% end %>
      </div>
    </div> <!-- </div>de project-zone -->
  <% end %>
  <!-- ----------------------------------------- -->
  <div class="project-zone tvs">
    <div class="cards">
      <% @tvs.each do |project| %>
      <% if project.photos.attached? %>
        <div class="entire-card">
          <%= link_to project_path(project) do %>
            <div class="card-project draw" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url('<%= cl_image_path project.photos.first.key %>');">
              <p class="project-name"><%= project.name %></p>
            </div>
          <% end %>
          <% if user_signed_in? %>
            <%= link_to project_path(project), method: :delete, data: {confirm: "certain de vouloir supprimer ce projet?"} do %>
              <i class="far fa-times-circle delete-project"></i>
            <% end %>
          <% end %>
        </div>
      <% else %>
        <% if project.illustrations.any? %>
          <div class="entire-card">
            <%= link_to project_path(project) do %>
              <div class="card-project draw" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url('<%= cl_image_path project.illustrations.first.photos.first.key %>');">
                <p class="project-name"><%= project.name %></p>
              </div>
            <% end %>
            <% if user_signed_in? %>
              <%= link_to project_path(project), method: :delete, data: {confirm: "certain de vouloir supprimer ce projet?"} do %>
                <i class="far fa-times-circle delete-project"></i>
              <% end %>
            <% end %>
          </div>
        <% else %>
          <div class="entire-card">
            <%= link_to project_path(project) do %>
              <div class="card-project draw" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url(https://images.unsplash.com/photo-1544981037-82284dcf0a0f?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=500&q=60);">
                <p class="project-name"><%= project.name %></p>
              </div>
            <% end %>
            <% if user_signed_in? %>
              <%= link_to project_path(project), method: :delete, data: {confirm: "certain de vouloir supprimer ce projet?"} do %>
                <i class="far fa-times-circle delete-project"></i>
              <% end %>
            <% end %>
          </div>
        <% end %>
      <% end %>
    <% end %>
    </div>
  </div>
  <!-- ------------------------------------- -->
  <div class="project-zone fashions">
    <div class="cards">
      <% @fashions.each do |project| %>
      <% if project.photos.attached? %>
        <div class="entire-card">
          <%= link_to project_path(project) do %>
            <div class="card-project draw" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url('<%= cl_image_path project.photos.first.key %>');">
              <p class="project-name"><%= project.name %></p>
            </div>
          <% end %>
          <% if user_signed_in? %>
            <%= link_to project_path(project), method: :delete, data: {confirm: "certain de vouloir supprimer ce projet?"} do %>
              <i class="far fa-times-circle delete-project"></i>
            <% end %>
          <% end %>
        </div>
      <% else %>
        <% if project.illustrations.any? %>
          <div class="entire-card">
            <%= link_to project_path(project) do %>
              <div class="card-project draw" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url('<%= cl_image_path project.illustrations.first.photos.first.key %>');">
                <p class="project-name"><%= project.name %></p>
              </div>
            <% end %>
            <% if user_signed_in? %>
              <%= link_to project_path(project), method: :delete, data: {confirm: "certain de vouloir supprimer ce projet?"} do %>
                <i class="far fa-times-circle delete-project"></i>
              <% end %>
            <% end %>
          </div>
        <% else %>
          <div class="entire-card">
            <%= link_to project_path(project) do %>
              <div class="card-project draw" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url(https://images.unsplash.com/photo-1544981037-82284dcf0a0f?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=500&q=60);">
                <p class="project-name"><%= project.name %></p>
              </div>
            <% end %>
            <% if user_signed_in? %>
              <%= link_to project_path(project), method: :delete, data: {confirm: "certain de vouloir supprimer ce projet?"} do %>
                <i class="far fa-times-circle delete-project"></i>
              <% end %>
            <% end %>
          </div>
        <% end %>
      <% end %>
    <% end %>
    </div>
  </div>
  <!-- -------------------------------------- -->
  <div class="project-zone lives">
    <div class="cards">
      <% @lives.each do |project| %>
      <% if project.photos.attached? %>
        <div class="entire-card">
          <%= link_to project_path(project) do %>
            <div class="card-project draw" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url('<%= cl_image_path project.photos.first.key %>');">
              <p class="project-name"><%= project.name %></p>
            </div>
          <% end %>
          <% if user_signed_in? %>
            <%= link_to project_path(project), method: :delete, data: {confirm: "certain de vouloir supprimer ce projet?"} do %>
              <i class="far fa-times-circle delete-project"></i>
            <% end %>
          <% end %>
        </div>
      <% else %>
        <% if project.illustrations.any? %>
          <div class="entire-card">
            <%= link_to project_path(project) do %>
              <div class="card-project draw" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url('<%= cl_image_path project.illustrations.first.photos.first.key %>');">
                <p class="project-name"><%= project.name %></p>
              </div>
            <% end %>
            <% if user_signed_in? %>
              <%= link_to project_path(project), method: :delete, data: {confirm: "certain de vouloir supprimer ce projet?"} do %>
                <i class="far fa-times-circle delete-project"></i>
              <% end %>
            <% end %>
          </div>
        <% else %>
          <div class="entire-card">
            <%= link_to project_path(project) do %>
              <div class="card-project draw" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url(https://images.unsplash.com/photo-1544981037-82284dcf0a0f?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=500&q=60);">
                <p class="project-name"><%= project.name %></p>
              </div>
            <% end %>
            <% if user_signed_in? %>
              <%= link_to project_path(project), method: :delete, data: {confirm: "certain de vouloir supprimer ce projet?"} do %>
                <i class="far fa-times-circle delete-project"></i>
              <% end %>
            <% end %>
          </div>
        <% end %>
      <% end %>
    <% end %>
    </div>
  </div>
  <!-- -------------------------------------------- -->
  <div class="project-zone events">
    <div class="cards">
      <% @events.each do |project| %>
      <% if project.photos.attached? %>
        <div class="entire-card">
          <%= link_to project_path(project) do %>
            <div class="card-project draw" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url('<%= cl_image_path project.photos.first.key %>');">
              <p class="project-name"><%= project.name %></p>
            </div>
          <% end %>
          <% if user_signed_in? %>
            <%= link_to project_path(project), method: :delete, data: {confirm: "certain de vouloir supprimer ce projet?"} do %>
              <i class="far fa-times-circle delete-project"></i>
            <% end %>
          <% end %>
        </div>
      <% else %>
        <% if project.illustrations.any? %>
          <div class="entire-card">
            <%= link_to project_path(project) do %>
              <div class="card-project draw" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url('<%= cl_image_path project.illustrations.first.photos.first.key %>');">
                <p class="project-name"><%= project.name %></p>
              </div>
            <% end %>
            <% if user_signed_in? %>
              <%= link_to project_path(project), method: :delete, data: {confirm: "certain de vouloir supprimer ce projet?"} do %>
                <i class="far fa-times-circle delete-project"></i>
              <% end %>
            <% end %>
          </div>
        <% else %>
          <div class="entire-card">
            <%= link_to project_path(project) do %>
              <div class="card-project draw" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url(https://images.unsplash.com/photo-1544981037-82284dcf0a0f?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=500&q=60);">
                <p class="project-name"><%= project.name %></p>
              </div>
            <% end %>
            <% if user_signed_in? %>
              <%= link_to project_path(project), method: :delete, data: {confirm: "certain de vouloir supprimer ce projet?"} do %>
                <i class="far fa-times-circle delete-project"></i>
              <% end %>
            <% end %>
          </div>
        <% end %>
      <% end %>
    <% end %>
    </div>
  </div>
  <!-- ----------------------------------------------- -->
</div> <!-- end of container -->
